export enum TaskStatus {
  Draft = 'draft',
  Requested = 'requested',
  Received = 'received',
  Accepted = 'accepted',
  Rejected = 'rejected',
  Ready = 'ready',
  Cancelled = 'cancelled',
  InProgress = 'in-progress',
  OnHold = 'on-hold',
  Failed = 'failed',
  Completed = 'completed',
  EnteredInError = 'entered-in-error',
}
