export enum MedicationRequestStatus {
  Active = 'active',
  OnHold = 'on-hold',
  Cancelled = 'cancelled',
  Completed = 'completed',
  EnteredInError = 'entered-in-error',
  Stopped = 'stopped',
  Draft = 'draft',
  Unknown = 'unknown',
}
