export enum ActionRelationshipType {
  BeforeStart = 'before-start',
  Before = 'before',
  BeforeEnd = 'before-end',
  ConcurrentWithStart = 'concurrent-with-start',
  Concurrent = 'concurrent',
  ConcurrentWithEnd = 'concurrent-with-end',
  AfterStart = 'after-start',
  After = 'after',
  AfterEnd = 'after-end',
}
