export enum EventStatus {
  Preparation = 'preparation',
  InProgress = 'in-progress',
  NotDone = 'not-done',
  OnHold = 'on-hold',
  Stopped = 'stopped',
  Completed = 'completed',
  EnteredInError = 'entered-in-error',
  Unknown = 'unknown',
}
